---
import { getEntry } from 'astro:content';
import LandingLayout from '../layouts/LandingLayout.astro';

const entry = await getEntry('pages', 'en/compare-viewers');

if (!entry) {
  throw new Error('Content entry not found: en/compare-viewers');
}
---

<LandingLayout entry={entry} />
